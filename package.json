{
  "name": "todo",
  "version": "0.1.0",
  "description": "Personal task API with Discord capture, web UI, and e-ink output.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "init-db": "node scripts/init-db.js",
    "test": "node --test --test-concurrency=1 tests/*.test.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "express": "^4.21.2",
    "nanoid": "^5.1.5"
  }
}
